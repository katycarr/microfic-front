import React from 'react'
import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  view: {
    position: 'absolute',
    bottom: 0,
    flex: 1,
    alignSelf: 'stretch',
    right: 0,
    left: 0,
    flexDirection: 'row'
  },
  button: {
    flex:1
  }
})
